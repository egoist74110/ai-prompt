# 维护约束

这个仓库同时服务两类读者：**人**和**AI**。两类内容的目标不同，不能混着写。

## 1. 先判断这段内容写给谁

修改或新增文档前，先问一个问题：**这段文字会不会被 AI 作为执行指令加载？**

### 写给人看的

典型位置：

- 根目录 `README.md`
- `CONTRIBUTING.md`
- 各目录/Skill 的 `README.md`
- 面向维护者的说明、教程、背景、设计解释

要求：

- 默认使用**简体中文**。
- 用大白话，优先让维护者快速看懂“这是干什么、为什么这么做、怎么改”。
- 可以解释背景、举例、写迁移步骤和排障过程。
- 不要为了省 token 把人类文档压缩成难读的提示词。
- 如果某条规则真正约束 AI，不能只写在 README；必须同步落到对应的机器文档。

### 写给 AI 看的

典型位置：

- `router.md`
- `common.md`
- `models/*.md`
- `capabilities/*.md`
- `skills/*/SKILL.md`
- AI 会直接加载的 prompt / policy / workflow / instruction 文档

要求：

- 默认使用**英文**。
- 简洁、明确、可执行；删除背景故事、重复解释和面向人的铺垫。
- 用稳定的命令式约束，例如 `MUST` / `MUST NOT` / `ONLY` / `IF ... THEN ...`，但只在确实需要硬约束时使用。
- 一个规则只保留一个权威位置；其它机器文档尽量引用它，不复制长段内容。
- 保留行为语义，不做为了“英文更短”而产生的规则降级。
- 面向用户的最终输出语言仍由 `common.md` 等运行规则决定；“机器文档写英文”不等于要求 AI 对用户说英文。

## 2. 混合内容怎么处理

如果一项能力既需要给人解释，又需要给 AI 执行，**拆成两层**：

```text
README.md / CONTRIBUTING.md
  → 中文：用途、背景、维护方法、示例、排障

SKILL.md / capabilities/*.md / models/*.md
  → English: minimal executable rules
```

不要在同一个机器 Prompt 里先用中文讲一遍原理，再用英文写一遍规则。也不要把只有 AI 才需要的内部执行细节大量复制到 README。

配置文件中的自然语言字段按消费者判断：如果字段会进入模型上下文，按 AI 文档规则写英文；如果只是给维护者看的注释/说明，可使用中文。业务数据、专有名词、测试样例不因为这条约束被强制翻译。

## 3. 修改提示词时的流程

1. **定位权威规则。** 先确认应该修改 `common`、`router`、某个 model、capability 还是 skill，避免到处追加同义规则。
2. **判断受众。** 人类说明写中文；模型执行规则写英文。
3. **最小修改。** 不顺手重写无关 Prompt，不扩大行为范围。
4. **检查重复和冲突。** 新规则若与已有规则重叠，优先合并或引用，而不是再复制一份。
5. **检查语义。** 对 AI 文档重点检查 trigger、MUST/MUST NOT、失败路径、完成条件是否仍然明确。
6. **更新人类说明。** 如果行为、架构或维护方式发生变化，更新对应 README/CONTRIBUTING，让人能知道发生了什么。
7. **运行仓库检查。** 至少执行现有 portability/index/compile 等检查；机器文档语言边界由仓库 guard 自动检查。

## 4. 文件分类原则

不要仅凭 `.md` 后缀判断语言。**按用途判断。**

| 类型 | 默认语言 | 目标 |
| --- | --- | --- |
| README / CONTRIBUTING | 简体中文 | 人容易理解和维护 |
| Router / common / model prompt | English | AI 低 token、明确执行 |
| Capability instruction | English | AI 执行规则 |
| `SKILL.md` | English | AI 技能工作流 |
| Reference / design doc | 看主要消费者 | 人读则中文，AI 加载则英文 |
| 配置中的模型指令字段 | English | 会进入模型上下文 |
| 业务数据 / fixture / 专有名词 | 保持原义 | 不机械翻译 |

如果无法判断一个文件属于哪一类，先看谁会读取它。**消费者决定语言，不是目录名决定语言。**

## 5. 不允许的做法

- 不允许新增中文 AI 指令并长期留在机器 Prompt 中。
- 不允许为了“统一英文”把面向维护者的 README 也改成生硬英文。
- 不允许只改 README 就认为 AI 行为已经改变。
- 不允许同一条完整规则在人类文档和多个 AI Prompt 中反复复制；人类文档解释，AI 文档执行。
- 不允许机械翻译代码、命令、路径、API 字段、业务文案、fixture 或专有名词。
- 不允许语言清理改变原有执行语义。

## 6. 一句话原则

> **给人看的：中文、大白话、讲清楚。给 AI 看的：英文、短、明确、可执行。谁消费，谁决定写法。**
